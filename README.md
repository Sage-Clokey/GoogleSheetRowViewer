# Google Sheet Row Viewer

Python GUI application for viewing and interacting with Google Sheets rows.

## Contents
- `sheet_gui.py` — Main GUI application
- `credentials.json` — Google Sheets API credentials (not committed)

## Setup
```bash
pip install gspread oauth2client tkinter
python sheet_gui.py
```
